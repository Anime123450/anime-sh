"""Stable domain core: models, ports, errors, ranking.

Nothing in this package may import from `app`, `infra`, `providers`,
`resolvers`, `cli`, or `tui`. Enforced by the import-linter contract in CI.
"""
